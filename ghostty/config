font-family = "CaskaydiaMono Nerd Font"
font-feature = +calt
#font-thicken = true

# kill the leftover-pixel strip at the window edge
#window-padding-x = 0
window-padding-y = 0 
window-padding-balance = true
window-padding-color = extend

# Left Option sends Meta (Esc-prefixed) for tmux M- bindings.
# Right Option still types special chars (é, etc.).
macos-option-as-alt = left

macos-titlebar-style = transparent
macos-titlebar-proxy-icon = hidden

title = " "
background-opacity = 0.89

# Active theme. Written by `bin/theme`; gitignored so switching themes never
# dirties this file. The `?` makes it optional (a fresh clone has no theme.conf)
# and being last means its `theme =` wins.
config-file = ?theme.conf
