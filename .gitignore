.DS_Store
settings.local.json

# Active theme, written by bin/theme (runtime state, not source).
ghostty/theme.conf

# Per-machine hotkey bindings (see bindings.local.lua.template).
hammerspoon/bindings.local.lua

# Hammerspoon-managed Spoon installs (runtime, not source).
hammerspoon/Spoons/
